# Dependencies and build output
node_modules
.next
out
.vercel

# Lockfile: pnpm owns its formatting, reformatting it causes noisy diffs
pnpm-lock.yaml

# Generated by Next.js on every build
next-env.d.ts
*.tsbuildinfo

# Environment files: prettier has no parser for these and would mangle them
.env
.env.*

# Static assets served verbatim
public

# pnpm settings file, kept in pnpm's own layout
pnpm-workspace.yaml
